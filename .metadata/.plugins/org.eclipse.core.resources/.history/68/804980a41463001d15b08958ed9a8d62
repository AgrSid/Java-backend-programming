package Day4Q3;

import java.util.Scanner;

public class Empmain {
	public static void main(String []args) {
	Scanner s1 =new Scanner(System.in);
	 {
		
		
		int num=s1.nextInt();
		int salary=s1.nextInt();
		Hourlyemp h=new Hourlyemp(salary);
		h.computegrosspay(salary);
		h.computenetpay(salary);
		Salaryemp s=new Salaryemp(salary);
		s.computegrosspay(salary);
		s.computenetpay(salary);
		switch(num) {
		case 1:
			System.out.println("Temporary emp");
			h.print();
			break;
		case 2:
			System.out.println("Salaried emp");
			s.print();
		
		}
		}
	}
}
